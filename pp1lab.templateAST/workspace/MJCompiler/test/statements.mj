//Test2 statements

program MJProgram

int number1, number2;
int array[];

class MyClass extends A{
int a; int b;

{
	void myFunc(int a, int b){
	    return;
	}
}
}

{
// do-while, read, print, if-else

   int max(int array) int i; int array[]; int value; {
	i = 2 + ;
    max = 0;
	if(array[0] == 0)
	   print(0);
	else if(array[0] == 1)
	   print(2);
	else
	    array[0] = 2;	

	do{

	    if(array[i] > max)
	    {
		max = array[i];
		print(i);

	    } else{
		print(array[i],2);
		print(max);	
	    }

	      if( i == 4 )
		break;
					 	
	    i++;
			
	}while(i < 5);
	
	read(value);
        if(value > max && value <= 55 || (value/2 + (max%2)*5*2 +5) == 60 || value != (max % 4))
	return value;

        return max;
   }
	
//switch, read

    int main() {
	
	i = 0;
        array = new int[5];
	myObj = new MyClass;
	do{
	    read(value);
            array[i] = value;
	    i++;
	}while(i < 5);

	value = max(array);
	switch(value%9){
	  case 1: print(max); print(array[0]);
          case 2: max = max*2; print(max);
          case 3: max = max * 5; print(max % 2);
	}

	return 0;
	
    }
	
}